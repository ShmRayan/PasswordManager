@model PasswordManager.Models.PasswordEntry

@{
    ViewData["Title"] = "Details";
    string decrypted = "Format invalide";
    if (!string.IsNullOrEmpty(Model.EncryptedPassword) &&
        PasswordManager.Helpers.EncryptionHelper.IsBase64String(Model.EncryptedPassword))
    {
        decrypted = PasswordManager.Helpers.EncryptionHelper.Decrypt(Model.EncryptedPassword);
    }
}

<h1>Details</h1>

<div>
    <h4>PasswordEntry</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ServiceName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ServiceName)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Username)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Username)
        </dd>

        <dt class="col-sm-2">
            Mot de passe
        </dt>
        <dd class="col-sm-10">
            @decrypted
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Notes)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Notes)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CreatedAt)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CreatedAt)
        </dd>

        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.UserId)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.UserId)
        </dd>
    </dl>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
